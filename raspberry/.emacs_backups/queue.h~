#ifndef QUEUE_H
#define QUEUE_H

#include "node.h"

class Queue {
private:
    Node* head;

public:
    Queue();
    ~Queue();
    void insertLog(const SystemData& systemData);
    SystemData readFirstLog();
    void removeFirstLog();
    void listAllLogs();
    bool isEmpty() const;
};

#endif

